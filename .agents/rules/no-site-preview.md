# No Site Preview

Do NOT use the `browser_subagent` tool for site preview or any browser-based verification in this project. Never open localhost or any project URLs in the browser. Use other methods (e.g., reading source code, checking server logs, running CLI commands) to verify changes instead.
